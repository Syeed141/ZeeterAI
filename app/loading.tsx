import LoadingSpinner from "@/components/LoadingSpinner";

export default function Loading() {
  return (
    <LoadingSpinner className="min-h-[calc(100vh-76px)] md:min-h-[calc(100vh-56px)] lg:min-h-[calc(100vh-80px)]" />
  );
}
